<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Redirige a index.php si la solicitud no es un archivo o directorio
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
